---
export type Props = {
  heading: string;
};

const { heading } = Astro.props;
---

<section class="py-5">
    <h2 class="fs-1 fw-bold text-center mb-4">{heading}</h2>
    <hr class="text-secondary" />
    <slot/>
</section>
