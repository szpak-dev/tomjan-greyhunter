---
export type Props = {
  heading: string;
};

const { heading } = Astro.props;
---

<section class="py-5">
    <h2 class="fs-4 fw-bold mb-4">{heading}</h2>
    <hr class="text-secondary" />
    <slot/>
</section>
